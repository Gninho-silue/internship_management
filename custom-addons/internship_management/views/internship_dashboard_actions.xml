<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- ================================= -->
        <!-- ROLE-BASED DASHBOARD CLIENT ACTIONS -->
        <!-- ================================= -->

        <record id="action_internship_dashboard" model="ir.actions.client">
            <field name="name">Executive Dashboard</field>
            <field name="tag">internship_role_dashboard</field>
            <field name="target">current</field>
            <field name="params">{}</field>
        </record>

        <record id="action_internship_student_dashboard" model="ir.actions.client">
            <field name="name">Student Dashboard</field>
            <field name="tag">internship_role_dashboard</field>
            <field name="target">current</field>
            <field name="params">{}</field>
        </record>

        <record id="action_internship_supervisor_dashboard" model="ir.actions.client">
            <field name="name">Supervisor Dashboard</field>
            <field name="tag">internship_role_dashboard</field>
            <field name="target">current</field>
            <field name="params">{}</field>
        </record>

        <record id="action_internship_company_dashboard" model="ir.actions.client">
            <field name="name">Company Dashboard</field>
            <field name="tag">internship_role_dashboard</field>
            <field name="target">current</field>
            <field name="params">{}</field>
        </record>

        <record id="action_internship_admin_dashboard" model="ir.actions.client">
            <field name="name">Admin Dashboard</field>
            <field name="tag">internship_role_dashboard</field>
            <field name="target">current</field>
            <field name="params">{}</field>
        </record>

        <!-- ================================= -->
        <!-- MENU ITEM -->
        <!-- ================================= -->

        <menuitem id="menu_internship_dashboard"
                  name="Dashboard"
                  parent="menu_internship_main"
                  action="action_internship_dashboard"
                  sequence="1"
                  web_icon="internship_management,static/description/icon.png"/>

    </data>
</odoo>
