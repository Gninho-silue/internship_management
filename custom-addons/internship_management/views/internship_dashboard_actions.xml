<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- ================================= -->
        <!-- FIXED DASHBOARD ACTIONS -->
        <!-- ================================= -->
        
        <!-- Remove the duplicate dashboard menu that causes the error -->
        <!-- The dashboard is already defined in internship_menus.xml -->
        
        <!-- Keep only these role-specific dashboard actions -->
        <record id="action_internship_executive_dashboard" model="ir.actions.client">
            <field name="name">Executive Dashboard</field>
            <field name="tag">internship_role_dashboard</field>
            <field name="target">current</field>
            <field name="params">{"role": "executive"}</field>
        </record>

        <record id="action_internship_student_dashboard" model="ir.actions.client">
            <field name="name">Student Dashboard</field>
            <field name="tag">internship_role_dashboard</field>
            <field name="target">current</field>
            <field name="params">{"role": "student"}</field>
        </record>

        <record id="action_internship_supervisor_dashboard" model="ir.actions.client">
            <field name="name">Supervisor Dashboard</field>
            <field name="tag">internship_role_dashboard</field>
            <field name="target">current</field>
            <field name="params">{"role": "supervisor"}</field>
        </record>

        <record id="action_internship_admin_dashboard" model="ir.actions.client">
            <field name="name">Admin Dashboard</field>
            <field name="tag">internship_role_dashboard</field>
            <field name="target">current</field>
            <field name="params">{"role": "admin"}</field>
        </record>

        <!-- ================================= -->
        <!-- NO MENU ITEMS HERE -->
        <!-- Menu items are defined in internship_menus.xml -->
        <!-- ================================= -->
        
    </data>
</odoo>
